tasks:
    webpack: >
        $bgShell watchify {{watchify.input}}
        -v -d
        -o {{watchify.output}}

config:
    watchify:
        input: 'test/fixtures/js/app.js'
        output: 'test/fixtures/js/app.watchify.js'
