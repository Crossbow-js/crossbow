watch:
  tasks:
    ts:
      before: ['compile']
      'src': ['test.js', 'compile']

tasks:
  compile: >
    @npm tsc
    src/** src/adaptors/** src/reporters/**
    --module commonjs
    --outDir dist
